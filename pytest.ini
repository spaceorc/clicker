[pytest]
markers =
    integration: slow integration tests that call real LLM APIs and browser (deselect with '-m "not integration"')
    unit: fast unit tests with no external dependencies

asyncio_mode = auto
